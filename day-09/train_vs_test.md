# Train-Test Split

Train test split is a function used to divide a dataset into two parts.


The model learns patterns from the training set and is evaluated on the testing set.




If we train and test on the same data, the model may simply memorize the dataset.

Train-Test Split helps us check whether the model can make predictions on unseen data.



## Common Ratio

- 80% Training + 20% Testing