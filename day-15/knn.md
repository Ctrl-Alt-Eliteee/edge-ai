K Nearest Neigbors:

from sklearn.neighbors import KNeighborsClassifier

here :

k = number of
Nearest  = closest
Neighbors = Data points


KNN simply looks at the nearest training examples and decides based on them.