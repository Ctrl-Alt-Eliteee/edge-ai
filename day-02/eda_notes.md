EDA:
exploration of dataset by observing the dataset 
before starting to train any model  in ML  we should do EDA as it is very necessary as an engineer we should first clean the dataset
In dataset we should check rows columns missing values shape etc
Missing values are data entries that are not available or not recorded in the datase
Duplicate rows are rows that appear more than once in a dataset.