AI vs ML vs DL



ML comes under AI and DL is subset of ML



deep learning means neural netwroks along with deep layers and every deep learning is machine learning but every machine learning is not deep learning
every result is not known to us so we cannot manually go through a dataset if it contains too much dataset values that's why ML exists



generalization means though the model does not know the exact value of all the given data it can predict the approx value of it



numpy is a library used to reduce teh errors and increase the accuracy of the output or model and it is not lengthy like describing a whole list and by using numpy library we can do operations over too many dataset using the functions



mean() is used for avg of all values



max() highest value in the array



min() smallest value in the array


