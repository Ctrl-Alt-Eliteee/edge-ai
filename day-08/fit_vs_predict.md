Fit:


fit() method teaches the model  using training data.


syntax-


model.fit(x,y)

x --> features
y --> labels


Predict:



predict() method uses the trained model to make predictions on new data.


syntax-

model.predict(x_new)

